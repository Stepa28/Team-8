services:
  gateway:
    image: ${DOCKER_REGISTRY-}gateway
    build:
      context: .
      dockerfile: Api/Dockerfile
    networks:
      - def

networks:
  def:
    external: true
    name: Team8-network
